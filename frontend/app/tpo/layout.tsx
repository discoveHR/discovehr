import type { ReactNode } from "react";
import "../../styles/company-dashboard.css";
import "../../styles/tpo-dashboard.css";

export default function TpoLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
