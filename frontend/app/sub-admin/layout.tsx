import type { ReactNode } from "react";
import "../../styles/company-dashboard.css";
import "../../styles/sub-admin-dashboard.css";

export default function SubAdminLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
