import type { ReactNode } from "react";
import "../../styles/institution-dashboard.css";

export default function InstitutionLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
