NEXT_PUBLIC_API_URL=/frappe
# Optional: pin Frappe URL for the Next.js proxy (server-side only).
# On Windows + WSL2, http://127.0.0.1:8000 usually works without setting this.
# NEXT_FRAPPE_BACKEND_URL=http://127.0.0.1:8000
